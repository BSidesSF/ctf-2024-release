apiVersion: v1
kind: Service
metadata:
  labels:
    app: cwa
  name: cwa
  namespace: default
spec:
  ports:
  - name: challenge
    port: 5432
    protocol: TCP
    targetPort: 5432
  selector:
    app: cwa
  type: NodePort
