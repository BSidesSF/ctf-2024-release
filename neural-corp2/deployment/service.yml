apiVersion: v1
kind: Service
metadata:
  labels:
    app: neural-corp2
  name: neural-corp2
  namespace: default
spec:
  ports:
  - name: challenge
    port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: neural-corp2
  type: NodePort
